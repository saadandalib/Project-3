CREATE TABLE restaurants (
	
	name varchar,
	address varchar,
	city varchar,
	state varchar,
	postal_code varchar,
	latitude numeric,
	longitude numeric,
	stars numeric,
	review_count integer,
	categories varchar,
	category varchar

);
